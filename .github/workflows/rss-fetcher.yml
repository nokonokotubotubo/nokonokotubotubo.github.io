# .github/workflows/rss-fetcher.yml (カスタム実装版)
name: RSS Feed Fetcher - Custom Implementation

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  fetch-rss:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install feedparser requests beautifulsoup4 lxml

    - name: Custom RSS Fetch Script
      run: |
        python3 << 'EOF'
        import feedparser
        import requests
        import json
        import os
        from datetime import datetime
        import time

        # 検索結果[2][6]に基づくUser-Agent設定
        headers = {
            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
            'Accept': 'application/rss+xml, application/xml, text/xml, */*',
            'Accept-Language': 'ja,en-US;q=0.9,en;q=0.8',
            'Accept-Encoding': 'gzip, deflate, br',
            'Cache-Control': 'no-cache',
            'Pragma': 'no-cache'
        }

        # 動作確認済みRSSフィード一覧
        feeds = [
            {
                'id': 'nhk-news',
                'name': 'NHKニュース',
                'url': 'https://www3.nhk.or.jp/rss/news/cat0.xml',
                'description': 'NHK 主要ニュース',
                'color': '#2E7D32'
            },
            {
                'id': 'gigazine',
                'name': 'GIGAZINE',
                'url': 'https://gigazine.net/news/rss_2.0/',
                'description': 'GIGAZINE テック記事',
                'color': '#FF4081'
            },
            {
                'id': 'yahoo-news',
                'name': 'Yahoo!ニュース',
                'url': 'https://news.yahoo.co.jp/rss/topics/top-picks.xml',
                'description': 'Yahoo! 主要ニュース',
                'color': '#6F42C1'
            },
            {
                'id': 'itmedia',
                'name': 'ITmedia',
                'url': 'https://rss.itmedia.co.jp/rss/2.0/topstory.xml',
                'description': 'ITmedia トップストーリー',
                'color': '#4A90E2'
            }
        ]

        os.makedirs('data', exist_ok=True)
        successful_feeds = []

        for feed_config in feeds:
            try:
                print(f"📡 取得開始: {feed_config['name']}")
                
                # 検索結果[2]の手法でUser-Agent付きリクエスト
                session = requests.Session()
                session.headers.update(headers)
                
                response = session.get(feed_config['url'], timeout=30)
                response.raise_for_status()
                
                print(f"✅ HTTP {response.status_code}: {feed_config['name']}")
                
                # feedparserで解析
                feed = feedparser.parse(response.content)
                
                if feed.bozo:
                    print(f"⚠️ {feed_config['name']}: 軽微なパースエラーがありますが続行")
                
                # エントリ変換（検索結果[3][4]の出力形式に準拠）
                entries = []
                for entry in feed.entries[:5]:
                    # 画像URL抽出
                    thumbnail = ''
                    if hasattr(entry, 'media_thumbnail') and entry.media_thumbnail:
                        thumbnail = entry.media_thumbnail[0].get('url', '')
                    elif hasattr(entry, 'enclosures'):
                        for enc in entry.enclosures:
                            if enc.get('type', '').startswith('image/'):
                                thumbnail = enc.get('href', '')
                                break
                    
                    entry_data = {
                        'id': getattr(entry, 'id', entry.link),
                        'title': entry.title,
                        'link': entry.link,
                        'description': getattr(entry, 'summary', ''),
                        'published': getattr(entry, 'published', datetime.now().isoformat()),
                        'thumbnail': thumbnail
                    }
                    entries.append(entry_data)
                
                # 検索結果[3][4]の形式で出力
                output_data = {
                    'title': feed.feed.get('title', feed_config['name']),
                    'link': feed.feed.get('link', ''),
                    'description': feed.feed.get('description', feed_config['description']),
                    'generator': feed.feed.get('generator', ''),
                    'language': feed.feed.get('language', 'ja'),
                    'published': datetime.now().isoformat(),
                    'entries': entries
                }
                
                # ファイル保存
                output_file = f"data/{feed_config['id']}.json"
                with open(output_file, 'w', encoding='utf-8') as f:
                    json.dump(output_data, f, ensure_ascii=False, indent=2)
                
                # メタデータ用に記録
                successful_feeds.append({
                    'id': feed_config['id'],
                    'name': feed_config['name'],
                    'description': feed_config['description'],
                    'file': f"{feed_config['id']}.json",
                    'color': feed_config['color']
                })
                
                print(f"✅ {feed_config['name']}: {len(entries)}件取得完了")
                
            except requests.exceptions.RequestException as e:
                print(f"❌ {feed_config['name']}: ネットワークエラー - {e}")
            except Exception as e:
                print(f"❌ {feed_config['name']}: 処理エラー - {e}")
                
            # レート制限対策
            time.sleep(1)

        # メタデータ生成
        metadata = {
            'lastUpdated': datetime.now().isoformat(),
            'totalFeeds': len(successful_feeds),
            'feeds': successful_feeds
        }

        with open('data/feeds-meta.json', 'w', encoding='utf-8') as f:
            json.dump(metadata, f, ensure_ascii=False, indent=2)

        print(f"\n📊 最終結果: {len(successful_feeds)}個のフィードが正常に取得されました")
        
        if len(successful_feeds) == 0:
            print("❌ 全てのフィードの取得に失敗しました")
            exit(1)
        
        EOF

    - name: Validate Generated Files
      run: |
        echo "=== 生成ファイル検証 ==="
        for file in data/*.json; do
          if [ -f "$file" ]; then
            if jq empty "$file" 2>/dev/null; then
              entries=$(jq '.entries | length' "$file")
              title=$(jq -r '.title' "$file")
              echo "✅ $(basename "$file"): ${entries}件 - ${title}"
            else
              echo "❌ $(basename "$file"): JSON無効"
            fi
          fi
        done

    - name: Commit and Push
      uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: 'RSS feeds updated via custom script - $(date -u +"%Y-%m-%d %H:%M UTC")'
        file_pattern: 'data/*.json'
